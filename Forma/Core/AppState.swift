//
//  AppState.swift
//  Forma
//
//  Purpose:
//  - Holds global, shared app state accessible across all views.
//  - For now, it’s a clean placeholder; we’ll expand it as we connect modules.
//
//  Created by Forma.
//

import SwiftUI
import Combine

// MARK: - Global App State

final class AppState: ObservableObject {
    // MARK: - Shared Properties

    // Example: We can add shared data later like:
    // @Published var selectedDate: Date = Date()
    // @Published var userSettings: UserSettings = .default

    // Currently empty, ready for expansion.
}
